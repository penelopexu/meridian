# 更新日志 / Changelog

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 变更
- **历法改为只跟界面语言走**（此前是「地点优先、语言兜底」）。
  非中文界面不再显示农历，哪怕地点在中国 —— 实际用下来，英文界面里冒出一行
  「Lunar 丙午马年 七月初六 · 甲子」既读不懂也突兀。传统历法对看得懂的人才是信息。
  想看的人在下拉框里选一下即可。
  **节日地区仍然跟地点走** —— 节日是「那个地方发生什么事」，跟历法的性质不同

### 修复
- **手机上语言下拉框的文字是蓝色的**：iOS Safari 用系统蓝渲染 `<select>`，
  普通的 `color` 压不住，必须同时写 `-webkit-text-fill-color`。桌面端不受影响，所以一直没发现
- 下拉箭头的颜色还是换配色之前的旧值（`#CDB795` / `#7A5A3A`），跟蓝调时刻和玫瑰黎明都对不上，
  改为跟随各自的强调色
- 手机上下拉框和输入框字号小于 16px 时，iOS 聚焦会自动放大整页；统一到 16px 消除跳动

### 重命名
- 仓库与包名改为 **meridian**（原 `tianshi`）。所有文档、CI、部署说明里的
  地址已同步更新。中文名仍是「天时」

### 界面
- **英文名定为 Meridian**（子午线）—— 既是经线也是正午，把时区、太阳、日历三件事串在一起。
  中日韩界面仍显示「天时」，其余语言用 Meridian
- **配色改为两套**，对应一天的两端：**蓝调时刻 Blue Hour**（暗）与
  **玫瑰黎明 Rosy Dawn**（亮）。取消了独立的调色板开关，◐ 按钮直接在两者之间切
- 语言选择框前面加了地球图标 —— 下拉框里显示的是当前语言的名字，
  万一手滑切成看不懂的语言就找不到回去的入口，图标是语言无关的锚点
- 「当前天气」面板加了标题，与右侧的「天气 · 未来 7 天」区分开

### 界面（续）
- 「当前天气」面板拆成两个标题：顶部是**当前时间**，天气那一段单独标**当前天气**
- 「节日与假期」加了**地区下拉框**。默认仍跟随地点（在东京看日本节日），
  但所选地点的国家不在这 28 个里时会退回界面语言对应的地区，
  也可以手动指定。日历格子上的假期标记同步跟随

### 修复
- **大号数字颜色不统一**：时钟、温度、体感用的是 `--cream`（深色），
  而日视图的日号用的是 `oat→golden→caramel` 渐变、换算结果用的是 `--warm` ——
  这两个在亮色主题下都落在玫瑰色系，和旁边的深色大字明显不是一路。
  统一为 `--cream`，并加了测试钉住这六处必须同色
- **亮色主题下时钟、温度、体感这几个大字几乎看不见**：
  它们的基础规则用的是 `--cream`（这个别名在亮色下本就是深色，是对的），
  但另有 5 条亮色覆盖规则把它们改成了 `--night` —— 而 `--night` 在亮色下是背景色。
  覆盖规则是早期遗留，直接删掉即可
- **切换语言时地点名不跟着变**：收藏标签、世界时钟、时区下拉框、天气副标题、
  搜索建议全都跟随了。国名改用 `Intl.DisplayNames` 实时生成，九种语言各自正确
- **非中文界面看国外地点会冒出农历**：历法改为「地点优先、语言兜底」。
  北京显示农历（哪怕界面是英文），伦敦配英文界面则整行不出现，
  伦敦配中文界面仍显示农历
- 直辖市的地点行会重复显示省市（「北京 · 北京市 · 中国」），判重时先去后缀再比
- 无障碍测试会扫到 HTML 注释里的 `<select>`，产生假阳性；改为先剥注释

### 新增
- **中央气象台预警接入**（`https://www.nmc.cn/rest/findAlarm`）：中国大陆全灾种官方预警，
  精确到县级。接口只给一个标题字符串，省市县/灾种/级别全埋在里面，所以匹配地点靠解析标题
- **Bright Sky 接入**：转发德国气象局(DWD)官方预警，填上欧洲的一角
- **官方气象预警源，全部免 API Key**：香港天文台（含熱帶氣旋警告 1/3/8/9/10 號、
  黃紅黑暴雨）、美国国家气象局（飓风、龙卷、洪水、暴风雪等全类型）、
  中央气象台台风网（台风路径与强度）
- **台风预警**：显示与所选地点的球面距离、中心风力、气压、移动方向。
  距离分级为本项目推算，界面标为「推算」，不冒充官方级别
- 预警源改为**登记表 + 按地点自动择源**架构，加新源只需往表里加一项
- `scripts/probe-alert-sources.html`：预警源可用性探测页。
  用真正的 `fetch()` 在浏览器里测 CORS，并能区分「没有 CORS 头」和「网络不通」；
  可导出完整样本，供为非公开文档接口写解析时校准
- `npm run test:alert`（132 项）：级别与类型映射用**真实抓回的样本**，
  择源与边界用造样本 —— 真实接口平时返回「没有警告」，而有警告的分支恰恰最需要被守住

### 未接入及原因
- **澳大利亚气象局**：接口技术上可用，但返回值里明写
  "You must not use, copy or share it"，是明确的法律禁止
- **MeteoAlarm（欧洲）、WMO、印度气象局**：都不发跨域响应头，静态站点读不到返回值
- **日本气象厅**：接口可用但无公开文档，结构是多层嵌套的数字代码，且抓到的样本
  报告时间是三个月前，可靠性存疑
- **中央气象台 http 版**：已被 https 版取代（原先担心的混合内容问题不存在了）

### 修复
- NWS 的 `Wind Chill Advisory`（风寒）被归成了「大风」——它说的是冷不是风大。
  含歧义关键词的判断顺序不能随便调
- 香港天文台的适用范围原本用经纬度框判断，深圳市中心落在框内，
  会被错误署名为「香港天文台发布」。改为只认国家码
- 预警去重原本拿标题前两个字互相匹配，多语言下会失效
  （英文 "Heat Warning" 与中文「高温预警」前两字对不上，导致重复显示）。改为按类型去重

### 安全
- 内容安全策略拆成两套：PWA 版用标准的 `'self'` 形态，单文件版完全不依赖 `'self'`
  （`file://` 下源是 opaque，各浏览器对 `'self'` 的处理不一致）。两版都把
  `connect-src` 锁死到天气接口域名
- 新增 `public/_headers`：Cloudflare Pages / Netlify 的安全响应头
  （nosniff、no-referrer、Permissions-Policy 全关、HSTS）与 Service Worker
  缓存策略。GitHub Pages 不支持自定义响应头，页面内的 `<meta>` CSP 是其兜底

### 测试
- 新增 `npm run test:a11y`（94 项）：landmark 齐全、符号按钮有无障碍名、
  三个产物的 CSP 白名单与实际请求域名一致、阿拉伯语确实翻转 `<html dir>`
- `scripts/zh-regression.mjs` 改造为界面快照回归（`npm run test:ui`）：
  49 块渲染产物按 SHA-256 比对黄金快照，`--update` 更新、`--dump` 导出完整 HTML 供人工 diff
- 删除过时的 `_baseline-i18n.js.txt`（那是只有中文一种语言时留下的基线）

### 文档
- README 重写并新增英文版 `README.en.md`
- 新增 `docs/域名与部署.md`：GitHub Pages / Cloudflare Pages / js.org 三条路线

### 修复
- 构建脚本在 Windows 共享盘上第二次运行会因 `cpSync` 覆盖已有文件抛 EPERM 而失败，
  改为「读出来再写回去」

## [0.1.0] — 首个公开版本

首次开源发布。

### 日历
- 万年历，覆盖 **1900–2100**：农历、二十四节气、干支纪年、生肖
- **12 种历法**：农历走自研引擎；伊斯兰历、希伯来历、印度历、波斯历、佛历、
  科普特历、埃塞俄比亚历、韩国阴历、日本年号、民国纪年走浏览器内置 Intl
- 日 / 周 / 月 / **年**四种视图，年视图一屏看全年
- 公历 ↔ 农历双向互转，惯性滚轮选择器
- 中国法定节假日与调休补班**逐日内置**（依据国务院办公厅通知），
  未公布年份按法定节日推算并标注「预估」
- 28 个国家/地区的节日规则，含复活节推算，按所选地点自动切换

### 天气
- 当前实况、今明逐时曲线（15 分钟粒度）、未来 7 天、**过去 3 天实测**
- 海拔、紫外线、空气质量（PM2.5 / AQI）
- **穿衣建议**：体感温度分段模型 + 风、湿、日照、降水、海拔、昼夜温差修正，
  可按「怕冷 / 正常 / 怕热」整体校准
- **气象预警**：官方预警（需和风 Key）+ 按中国气象局阈值本地推算
- 历史天气可查到 **1940 年**：单日、区间折线图、历年同期对比与趋势
- 全球城市搜索，中英双索引并行查询；中国区县自动映射到所属地级市
- 时区换算，左右对称双向，自动处理夏令时
- 多地点收藏，主收藏默认显示

### 界面
- **9 种语言**：简中、繁中、英、日、韩、西、法、德、阿拉伯（含 RTL 布局）
- 两套调色板 × 明暗两种主题，全部文字对比度达 WCAG AA
- PWA，可安装到桌面/手机，日历部分**完全离线可用**
- 单文件 HTML 版，双击即用，无需服务器

### 工程
- 零运行时依赖，无打包器
- 测试：农历全量往返、历书锚点、离线核心、多历法、多语言，CI 自动跑
- 构建产物内嵌完整的第三方署名（OFL / CC BY）

### 已知问题
- 农历引擎与 ICU 的 `chinese` 日历在 1901–2099 有约 300 天日序不同、
  177 天仅闰月标号不同，涉及 13 个年份。凡能与公开历书核对的锚点本引擎均正确，
  但尚未建立完全独立的天文验证。明细见 `docs/lunar-vs-icu.tsv`，欢迎复核。
- 反向换算（历法 → 公历）目前只有农历实现，其他历法为单向显示。
- 和风天气数据源尚未经过真实 API 凭据的端到端验证。
