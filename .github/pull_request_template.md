## 改了什么

<!-- 一两句话说清楚。如果是修 bug，请说明根因而不只是现象。 -->

## 关联 issue

<!-- Closes #123 -->

## 自检

- [ ] `npm run check` 通过（语法 + 0 个全局重名）
- [ ] `npm test` 通过
- [ ] `npm run build` 成功
- [ ] 没有引入 npm 依赖
- [ ] 没有写死颜色（用了 CSS 变量）
- [ ] 没有写死文案（用了 `T()`，且 9 种语言都补齐了）
- [ ] 来自网络的字符串都过了 `esc()`

## 影响面

<!-- 改动会影响哪些语言 / 历法 / 主题 / 视图？在哪些组合下验证过？ -->

## 截图

<!-- 界面改动请附暗色和亮色两套截图 -->
